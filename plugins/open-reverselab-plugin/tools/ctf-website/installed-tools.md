# CTF Tools Installation Status

Run `.\scripts\ctf-website\ctf_toolcheck.ps1` to check, or `python scripts/misc/ai_toolcheck.py` for full audit.

To install missing tools:
```powershell
.\scripts\misc\install_tools.ps1 -CTF
```

Individual tool READMEs in this directory have manual download links.
